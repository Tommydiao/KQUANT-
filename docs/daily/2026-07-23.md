# 2026-07-23 - Planned Days 1-22: Data Trust, Strategy Safety, and Historical Replay

## Today's goal

Audit the actual local KQUANT runtime against the 84-day development plan and
freeze the product, strategy, and market-data contracts before changing
strategy behavior.

## Files changed

- `docs/current_system_audit.md`
- `docs/personal_live_mvp.md`
- `docs/strategy_specification.md`
- `docs/market_data_contract.md`
- `docs/daily/2026-07-23.md`
- `kquant/strategy_registry.py`
- `kquant/stock_store.py`
- `kquant/stock_signals.py`
- `kquant/validation_service.py`
- `tests/test_strategy_versions.py`
- `scripts/verify_all.ps1`
- `.github/workflows/ci.yml`
- `web/src/tradingFormatters.ts`
- `web/src/tradingFormatters.test.ts`
- `docs/KQUANT_84_DAY_CODEX_PLAN.md`
- `docs/longbridge_provider_audit.md`
- `kquant/market_store.py`
- `tests/test_market_candle_store.py`

## Completed

- Checked the active local branch, recent commits, working-tree state, core
  FastAPI routes, SQLite schema, startup script, test layout, and documentation
  inventory.
- Recorded the remote/default-branch versus local-branch release divergence.
- Confirmed the active dashboard is stock-only and has no account, broker,
  order, position, options, Binance, BTC, or ETH route.
- Recorded the precise strategy-version and data-contract gaps that block safe
  strategy changes.
- Defined the six-step personal manual-trading workflow and the non-negotiable
  no-broker/no-order product boundary.
- Frozen `swing_long_v1.0.0` as a deterministic daily-trend/1-hour-confirmation
  strategy specification, including scores, gates, risk plan, and versioning
  rule.
- Defined the source, timestamp, session, freshness, candle-state, fallback,
  storage, and degraded-mode market-data contract.
- Added immutable `strategy_versions` records with a deterministic SHA-256
  configuration hash and conflict protection against rewriting an existing
  version with altered parameters.
- Bound new scan runs, signal rows, feature rows, label-backtest summaries,
  Journal entries, strategy-validation trades, and validation summaries to a
  strategy version and configuration hash.
- Replaced the stale options-liquidity checklist item with a Journal-first
  manual-action check that matches the frozen product boundary.
- Updated the Windows launcher to use `.venv` before the legacy `.venv-win`
  path, and added a one-command verification workflow.
- Added frontend TypeScript linting, a Vitest baseline with two tests, and CI
  steps that run both before the production build.
- Created the 84-day progress board and documented that the legacy replay CLI
  is not yet canonical `swing_long_v1.0.0` validation evidence.
- Performed a credential-safe Longbridge provider audit: SDK is installed, but
  quote/depth entitlement remains unverified because no local credentials are
  configured; the runtime correctly stays in caution/read-only mode.
- Added a canonical market-candle store with source-observation lineage,
  fetch-time, provider-status, bar-state, adjustment-mode, and dataset-version
  fields. Existing cache reads continue using the legacy table while new work
  has a deduplicated audit source.

## Verification

- `git status --short --branch`: local branch is clean and three commits ahead
  of its locally configured `origin/main` reference.
- Confirmed `.venv-win\Scripts\python.exe` is unusable because it points to an
  old local Python path.
- Restored an isolated `.venv` and installed `.[dev]` successfully.
- `pytest tests/test_strategy_versions.py tests/test_strategy_validation_v3.py -q`:
  `6 passed`.
- `pytest` for the directly affected scan/analyze/Journal tests: `3 passed`.
- `pytest tests/test_dashboard.py tests/test_realtime_strategy.py
  tests/test_longbridge_runtime.py tests/test_market_clock.py -q`: `19 passed`
  with one upstream FastAPI/Starlette deprecation warning.
- Full `pytest -q`: `67 passed, 1 warning in 133.09s`; the warning is the
  upstream FastAPI/Starlette `TestClient` deprecation notice.
- `cd web && npm.cmd run build`: passed.
- `npm run lint && npm test && npm run build`: passed; Vitest reports `2 passed`.
- `python scripts/verify_read_only_boundary.py`: passed; 34 registered routes,
  no forbidden route.
- The full suite needs about 133 seconds on this machine, which exceeds the
  one-shot command display cap but is supported by `scripts/verify_all.ps1` and
  GitHub Actions.
- Longbridge self-check: market calendar and SQLite audit write passed; no
  credentials, quote, or depth entitlement were configured, so status was
  correctly `caution` with no buy-class data readiness.
- Canonical candle-store plus stale-cache/fixture regression tests: `3 passed`.

## Unresolved issues and risk

- The local runtime-validation branch has not been reconciled with the public
  GitHub default branch.
- The formal strategy-version registry and signal/validation bindings are not
  applied retroactively to old rows; existing data is intentionally marked
  `legacy_unversioned` rather than being relabelled.
- The current code still exposes legacy profiles. They need to be removed from
  the active product path or explicitly archived without altering past records.
- A credentialed Longbridge quote/depth smoke remains blocked until the owner
  deliberately adds rotated local credentials.

## Next recommended task

Implement the Day 11 corporate-action policy, then the point-in-time universe
and formal data-quality report. Do not alter strategy parameters or expand the
product scope.

## Commit

- `a006d7e feat(strategy): bind immutable strategy versions`
- `a5d36d9 chore(tooling): add repeatable verification workflow`
- `f3a9090 docs(plan): record first week baseline`
- Longbridge audit documentation commit pending.

## Continued Work: Days 8-22

- Added canonical candle lineage, conservative corporate-action cautions, and
  runtime universe snapshots. Historical replay is explicitly labelled
  survivorship-limited until an authorised historical membership feed exists.
- Added a machine-readable candle/realtime data-quality gate and deterministic
  fault injection for timeout, fallback, future-bar, and cache-write failure.
- Made `swing_long_v1` the only visible decision profile. Legacy profiles remain
  comparison-only and retain their own version identity.
- Added unified technical features, daily trend and hourly confirmation modules,
  a manual-plan risk assessment, explicit scoring factors/deductions, and 20
  deterministic safety regression cases.
- The strategy now has two deliberate immutable versions: `1.0.1` made scoring
  configuration explicit without changing its values; `1.1.0` added the
  deterministic hard-veto policy that forces a final `WAIT` conclusion.
- Added a historical signal reconstruction boundary that strips forming and
  future candles and records the exact cutoff and bar counts used.

## Continued Verification

- `tests/test_data_quality.py`: `3 passed` before fixture-source refinement;
  subsequent direct smoke covered the refined fixture block and candle API path.
- Fault matrix smoke covered provider timeout, Longbridge-to-Yahoo fallback,
  future timestamp, and SQLite cache-write failure.
- `tests/test_strategy_golden_cases.py -q`: `20 passed in 1.37s`.
- Historical replay test: `2 passed in 1.56s` (the terminal wrapper ended after
  printing the successful result because of its short display timeout).
- Focused smoke passed for technical features, trend analysis, hourly
  confirmation, manual risk assessment, configured scoring, hard vetoes, and
  fixture signal integration.

## Current Risks and Next Step

- The fresh changes have not yet had a new full `pytest -q` or frontend build;
  they must both be run before a release or GitHub push is described as green.
- Longbridge credentials remain intentionally absent, so quote/depth entitlement
  is still unverified and the local runtime must remain read-only/caution.
- Historical universe membership and a corporate-action feed are still missing;
  backtest results remain limited evidence rather than point-in-time proof.
- Next implementation target: execution-cost and liquidity scenarios for the
  backtest runner, followed by an aggregate data-quality report.

## Continued Commits

- `fb9731b feat(data): add canonical candle lineage`
- `b522b19 feat(data): snapshot runtime universes`
- `2171aae feat(data): gate market data quality`
- `14d6b9e feat(strategy): make swing long canonical`
- `bc06bd4 fix(strategy): enforce data quality gates`
- `64144d0 feat(strategy): unify technical feature contract`
- `6b17559 feat(strategy): add daily trend module`
- `c3d8f04 feat(strategy): add hourly confirmation module`
- `0e5f222 feat(risk): assess manual trade plans`
- `135147e feat(strategy): configure canonical scoring`
- `550113e feat(risk): enforce deterministic hard vetoes`
- `ee2d383 test(strategy): add golden safety cases`
- `06102b7 feat(backtest): reconstruct historical signals safely`

## Continued Work: Days 23-24

- Kept the existing next-bar-open, commission, slippage, gap-stop/gap-target,
  and same-bar stop-first replay rules as the baseline execution path.
- Added optimistic, baseline, and conservative execution-cost scenarios with
  price and average-dollar-volume liquidity adjustments. These are attached to
  replayed outcomes without replacing the baseline historical result.
- `tests/test_execution_costs.py`: `2 passed`.
- `python scripts/verify_read_only_boundary.py`: passed; 34 routes and no
  account, position, broker, or order-submission route.
- A fresh frontend production build was started but the terminal display timeout
  interrupted it before a final result was received. It remains pending, along
  with a fresh full `pytest -q`, before publication can be called green.

## Additional Commit

- `068fb51 feat(backtest): model execution cost scenarios`

## Continued Work: Days 25-48

- Added a deterministic cash-only portfolio replay with same-timestamp ranking,
  maximum position count, risk budgets, cash constraints, equity curve,
  drawdown, complete performance metrics, and SPY/QQQ/EMA benchmark references.
- Added reproducibility audits: stable input fingerprint, data snapshot hash,
  strategy/config hashes, runtime description, and JSON/Markdown reports.
- Added rolling chronological walk-forward windows, neighbouring-parameter
  replay, regime and concentration reports, bootstrap/Wilson confidence,
  approximate deflated-Sharpe check, and a research-only Evidence Score.
- Added a gated strategy-freeze manifest. It refuses to freeze a version unless
  a validation fingerprint exists and evidence reaches the configured threshold.
- Added the bounded daily candidate board (3 BUY SETUP / 7 WATCH), a manual
  plan normalizer, and a local-input-only position calculator.
- Added Decision Ledger, manual trade Journal, loss/error attribution and
  weekly personal review. These record manual observations only; they do not
  access a broker or submit an order.
- Added local operations foundations: SQLite schema version, idempotent task
  records/retries, notification queue with opt-in external delivery, operational
  events, verified backups, restore drill, production architecture, and runbook.

## Continued Verification

- `tests/test_portfolio_backtest.py tests/test_backtest_audit.py
  tests/test_strategy_validation_v3.py -q`: `6 passed`.
- `tests/test_validation_robustness.py -q`: `3 passed`.
- `tests/test_manual_workflow.py tests/test_dashboard.py -q`: `8 passed` with
  the existing upstream TestClient deprecation warning.
- `tests/test_operations.py tests/test_dashboard.py -q`: `9 passed` with the
  same upstream warning.
- A fresh full suite and frontend production build remain required before a
  release or GitHub push is called green.

## Current Operating Limits

- Longbridge credentials/depth entitlement remain an owner-run local audit.
- Current historical universe membership remains survivorship-limited.
- Strategy freeze and forward observation are gated by a real, reviewed
  validation report; the implementation does not auto-freeze the canonical
  strategy.
- PostgreSQL is not enabled: the migration contract is documented and SQLite is
  versioned, but a production adapter, staging parity suite, and restore drill
  are still a release gate.
