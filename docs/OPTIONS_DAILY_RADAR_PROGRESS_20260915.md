# 每日期权雷达：实施进度与真实证据

## 结论

本轮已经实际修改并验证代码，完成隔离数据库中的真实行情盘前补运行及盘中复核；尚未完成10个工作日全部任务，也未启用生产自动监控。

| 维度 | 当前结论 |
| --- | --- |
| 工程 | 受影响测试、统一构建、桌面/移动端检查通过；正式后台部署待批准 |
| 数据 | Longbridge 正股可读取；OpenAPI OPRA 未检测到；严格 BBO 不可用；审核事件覆盖仍为0 |
| 跟踪闭环 | 持久检查点、期限监控、跨日未退出记录、通知出站队列已实现；持续实盘时段和手机验收未完成 |
| 收益 | 期权模拟结果0，PERFORMANCE_UNPROVEN；不能报告策略胜率或盈利能力 |

## 已交付代码

- `options_expression.py`：接收时间改为请求完成后记录；保存请求/接收/原始成交时间，缺少时区不再直接当UTC。适配SDK 4.4.1平铺 OptionQuote 字段及方向枚举。按明确百分比单位转换IV，不再按数值大小猜单位。严格BBO仍关闭。
- `option_event_coverage.py` + migration15：独立期权事件覆盖、来源哈希、审核及有效期。缺失、过期、未来审核、阻断事件都不放行；不修改股票事件策略。
- `option_runtime.py` / `options_radar_supervisor.py`：复用原Supervisor，添加OS单实例锁、扫描锁、持久任务/事件/心跳。08:30按交易日运行，晚启动记录catch-up，收市后不补造盘前信号。扫描失败不阻塞独立期限监控线程。
- `options_radar.py`：当前机会按当日过滤；关注、未完成模拟和人工持仓跨日保留。退出提醒与状态事件去重；无exit_reminder的短波段使用既有expires_at兜底，不把提醒当退出成交。缺失报价接收时间不再补造。严格模拟仍要求决定后有效报价。
- 通知：SSE事件与出站记录持久化，推送网络请求移入独立投递线程；普通消息遵守静默/日限，风险例外保留。Web Push按订阅保存尝试，已发送不重发，累计最多三次，404/410停用。Telegram复用已有transport，错误不记录含凭据的URL。
- Gateway：新增美股快速`/api/health/live`探针，不以远程行情调用结果判断进程存活；拒绝HTML回退页冒充健康JSON。
- 前端：仅在期权设置补“每日雷达运行”、报告日期、下次扫描、手机订阅、通知授权与测试按钮，补计划通知深链接时间线；未更改导航、布局或策略。

## 真实行情证据

独立目录：`work/option_daily_20260915/`，不是原期权业绩库。

1. `provider_audit_02/audit.json`：2026-09-15 13:35:42—13:35:47 UTC，SDK4.4.1。三次SPY读取，耗时约0.937/0.063/0.062秒。
2. SDK原始成交时间是无时区datetime。沿用既有股票解析器将其解释为Asia/Shanghai后，事件比实际接收领先336.094/336.035/335.973秒。这个差值不是网络耗时，也不是完整8小时时区差。
3. 最初`provider_audit_01`暴露了期权解析器把无时区时间当UTC造成额外8小时误差的问题；该报告保留，但不能作为正常时钟证据。修复后的02保留raw值与单独的股票解释结果，不伪造native盘口时间。
4. Windows Time服务未运行；`w32tm /stripchart /computer:time.windows.com /samples:3 /dataonly`三次超时。该命令虽然进程退出码为0，时间校验仍失败。尚不能判定327/336秒来自主机或行情端，未校时。
5. OPRA未检测到；NVDA260928P215000.US合约读取不可用。SecurityDepth和PushDepth实际SDK属性只有asks/bids，无native事件时间。QuoteContext中未发现calendar/earnings/dividend方法，不等于长桥所有产品均无此能力；发行人/官方宏观覆盖仍待审核。
6. `real_cycle_01/result.json`：13:39:16.845 UTC真实补运行，11.062秒完成；run=`option-radar-de161b069347d2cedfb418a4`。1个NVDA Put观察机会、3个14–35DTE参考计划；不是三个期限组。
7. 13:47 UTC实际执行同库`intraday`，使用13:30与13:35两根完整5m。方向确认，但计划转为QUOTE_BLOCKED：OPRA、事件覆盖、同盘前时刻观察不足20天。原决定13:39:16未重写，报价证据0、模拟成交0。
8. 原生产库只读核查：8个运行、7个机会、7个计划；报价证据0、结果0、审核覆盖0、运行检查点0、心跳0、手机订阅0。已有9月15日报告不能当作新Supervisor已上线的证明。

## 验证

- 美股受影响回归：64 passed；包括期权、迁移、API、Longbridge只读订阅及既有实时指令。日志：`pytest-final.xml`。
- Gateway：6 passed，4项不相关测试未运行。日志：`gateway-tests.xml`。
- 前端：11 passed；`npm.cmd run build:unified`通过。
- 新测试覆盖v14升级保留既有计划/备份、双实例互斥、重启时段去重、晚启动、收市不补运行、失败扫描后继续跟踪、未确认人工退出恢复、通知静默风险例外、报价缺失与时区、平铺SDK字段、开盘5m连续性。
- 浏览器1440/375px中文、375px英文：新运行区可用，无页面横向溢出；等待移动侧栏收起动画完成后复查，按钮按语言换行。最终截图为`options-runtime-verified-1440.png`、`options-runtime-verified-375.png`、`options-runtime-verified-en-375.png`，保存在同一证据目录。未自动请求通知权限，未声称完成真iPhone锁屏测试。
- 只读边界：115条路由、禁止路由0；统一构建同时纳入已有密钥赋值模式扫描。该检查不是第三方全面渗透或凭据审计。
- `git diff --check`通过。无无关Crypto研究/训练或股票全量模型回放。

## 基线、变更副作用与恢复

- HEAD仍为`baac8d1fe3156be39f7b725d0ed9a20e68c2431f`；大量之前未提交内容保留，本轮未创建混合提交。
- 服务PID保持：8001=26856，8002=43044，8011=14872，8020=45196；另一个应用8010=36940未触碰。
- 验收发现原`verify_read_only_boundary.py`默认调用create_app，会初始化真实数据库；它于13:46:32UTC触发了migration15及应用初始化。已改为临时数据库并复测。迁移本身只添加表/索引及迁移记录，但没有迁移前全库快照，不能声称本次对主库完全无写入。
- 已完成当前v15备份及隔离恢复：`work/backups/kquant-us-20260915T135003Z.sqlite3`，SHA256 `8c8f419d6989db2dd9fbf28c6560132d7e727d438b8ddc9e6d754273b7b8bbd6`，integrity_check=ok，104表。它是迁移后恢复点，不是紧邻迁移前的回滚点。未回滚或覆盖在线库。
- `.env`由python-dotenv读取时报告第5行无法解析；没有输出该行或自动重写配置。当前凭据足以连接正股公开行情，但配置格式需独立核查。

## 未完成与下一交付

| 待办 | 阻塞范围与负责人 |
| --- | --- |
| 批准受控重启8002/8020并启用现有Supervisor | 用户确认后Codex部署；不动8001及Crypto采集。现在新前端已构建，旧后端尚未加载新代码 |
| 独立校验327/336秒时钟差，必要时确认校时 | 只阻塞依赖实时事件时序的确认/严格模拟；不是整体停工理由 |
| 审核12标的事件覆盖及官方宏观来源 | Codex核验来源、人工审核；不能导入假“无事件”放行 |
| OPRA与严格盘口原生时间 | 用户自行决定权限购买，Codex不购买；有OPRA也不等于当前SDK提供严格盘口时间 |
| 连续价格风险/重要消息再审核与期限监控的完整分离 | 当前新增独立线程专做期限提醒，仍需补真实消息与价格失效长跑验收；不夸大为全部风险闭环 |
| 持久通知从生产进程端到端投递 | 出站队列代码已测，真实订阅0，尚无送达证据；缺设备不阻塞后台编码 |
| Cloudflare受保护HTTPS与iPhone授权 | 本机未发现cloudflared进程，未验证可用外部保护入口；不建立无认证公网网站。用户完成主屏幕添加与授权 |
| 至少20真实交易日及每声称有效分组200结果 | 未完成，不压缩、不借用旧Paper/正股回测；原收益门槛不变 |

下一轮先受控部署并采集第一个真实自动交易日的运行证据，同时完成官方事件覆盖审核；不新增策略、不重做UI、不自动下单。

## 官方契约来源

- [Longbridge option quote与OpenAPI独立权限](https://open.longbridge.com/docs/quote/pull/option-quote)
- [Depth字段与OPRA要求](https://open.longbridge.com/docs/quote/pull/depth)
- [Greeks及百分比字段定义](https://open.longbridge.com/docs/quote/pull/calc-index)

详细命令见`OPTIONS_RADAR_RUNBOOK.md`。这里记录的是工程进度，不是期权策略盈利证明。
