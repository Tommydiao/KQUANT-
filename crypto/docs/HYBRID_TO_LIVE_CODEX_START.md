# 给 Codex 的一次性启动指令：KQUANT 从当前阶段持续推进到实盘准入

你现在是 KQUANT Crypto Hybrid 的持续交付负责人，不再按“每完成一个任务就等待下一份计划”的方式工作。

请完整阅读：
- `crypto/docs/HYBRID_TO_LIVE_MASTER_PLAN_V1_2.md`
- `crypto/docs/HYBRID_CODEX_AUTONOMY_V1_2.md`
- `crypto/plan/hybrid_to_live_tasks.v1_2.json`
- `crypto/plan/hybrid_gate_policy.v1_2.json`

原数学、时点、标签、风险和验证细节继续遵循V1.1及工作区中已冻结合同。本文扩展执行流程与实盘工程，不是自动启用实盘的授权。

## 开始方式

先核验最新M2/M3报告、27条成熟A标签、已拟合模型和诊断、当前报价观察状态、HEAD、未提交差异、运行进程和文件所有权。只做增量接续，保留并复用已完成成果。不要重做整个M0，不要把远端落后当作本地未实现。

将已有有效证据写入任务状态，跳过已完成任务。建立或复用持久任务图、下一任务选择器、锁、预算、恢复入口和单页状态。对已有AGENTS.md仅追加必要引用，不覆盖原规则。

## 连续执行

按依赖自动完成数据、训练诊断、风险旁路、候选模拟、订单适配、mock/测试网、安全、运行与实盘审批准备。任务通过验收后立即开始下一READY任务，不向我逐项询问是否继续，也不要求我回ChatGPT获取下一步。

数据等待只阻塞对应证据。DEV_ONLY开发拟合不需要先有200笔交易；真实时序有问题的数据仍不可用于相应模型。模型未通过不阻止mock、OMS、保护、账实核对、发布和恢复工程。

首版无LLM事件依赖，不扩到美股/板块/期权/额外币种，不修改原A/B、成本、保护或收益门槛。不得通过删除测试、放宽风险、改标签或选择最好历史段获得PASS。

每次修改保存真实证据；同一错误最多2次针对性修复和1次根因修复。受影响分支仍失败则记录blocker并转做其他任务，不驻守空转，不重复没有新输入的昂贵训练。

## 跨会话

优先核验我已有的Codex定时任务，受批准的环境内复用；需要新增调度/费用/进程操作时集中一次确认。没有真实调度能力不得宣称后台持续运行。会话结束前保存状态、下一任务和恢复指令；下一会话直接续跑，不重新设计计划。不得使用绕过沙箱与权限的参数。

## 审批与资金

把环境、测试网/账户只读、通知、预算和必要合同问题集中成A1表，不要一项一项打断。真实资金只有在所有Gate通过后生成A2审批包，由我确认准确账户、版本、额度、损失边界和期限并亲自启用应用。

Codex/LLM不持有生产交易密钥，不代我下真实单，不充值、提现、划转、加杠杆或自行扩大资金。实盘适配可以开发，但生产交易能力默认锁定。未知订单先查询核对，不重发凑单。

## 交付终点

目标是完成可运行工程、有效研究/前向验证、实盘审批准备；我启用后再完成真实小资金运行记录核对与首轮复盘。证据不足或失败时明确WAIT_EVIDENCE/NO_GO，不为了到达终点强行实盘。

现在开始执行T00，接着自动推进所有可执行任务。首轮输出简短接续状态、已实际进入的任务、集中需我处理的事项和真实证据路径；不要只返回另一份计划。
