# Hybrid V1.1 M0/M1 Work Log

## Scope

Read-only baseline audit and offline executable contracts. The prior dual-mode
sprint remains protected. No shared candidate source, collector, old evidence,
execution admission or UI changed in this Hybrid pass.

## Evidence

- HEAD baac8d1fe3156be39f7b725d0ed9a20e68c2431f; dirty candidate work retained.
- Original collector PIDs40088/36116 and candidate PID31772 observed live.
- Fresh read-only SQLite audit: A/B v4 completed, forward_A_frozen_v1 running,
  lease unexpired when read; no old lease acquired.
- New immutable output: outputs/hybrid_regime_v1/m0_inventory_20260905_01/
  baseline_reference_manifest.json, tracked_diff.patch. 46 source/doc/reference
  files hashed. No market rows were read by the audit script.
- Parent AGENTS checks through repository root returned absent.

## Commands Actually Run

1. Candidate six-file pytest suite: 196 passed in16.98s, exit0.
2. New test_hybrid_contracts.py before implementation: missing module, exit1.
3. Same new suite after implementation: 15 passed in0.08s, exit0.
4. `python scripts/audit_hybrid_baseline.py --output outputs/hybrid_regime_v1/m0_inventory_20260905_01`: exit0.
5. Full `python -m pytest -q`: 506 passed in64.94s, exit0. This includes the
   15 new contract cases; it does not establish unimplemented Hybrid runtime Gates.

## Actual New Files

hybrid_contracts.py, hybrid_identity.py, test_hybrid_contracts.py,
scripts/audit_hybrid_baseline.py; baseline audit, blockers, math/time/metric
specifications, experiment protocol, traceability and this log.

## Remaining

Full docs-read completion, baseline source/run comparison and fresh golden trace;
three-identity SQL constraints and atomic portfolio-version test; protection-lane
synthetic orchestration; numerical error and prior design checks; typed posterior
and censored labels; final section17.1 A-J delivery. M0/M1 is NOT complete.

The old Roll summarizer uses cumulative realized-R peak/trough; the candidate
summarizer uses exit_time/trade_id ordering and frozen BASE signal risk. Matching
the word drawdown does not yet prove original 10R denominator/ordering equivalence.
Keep B-10R open while tracing its exact source. No commit or push made.

Next unique objective: prove stale-evaluation rejection and one-entry-per-signal
in an isolated synthetic single-writer transaction contract, with protection
priority and no model wait. Do not wire that harness into any current runtime.

## First Delivery Update (Supersedes Remaining List Above)

- All pre-existing docs were read, including architecture sections and daily
  reports; their historical claims were treated as context, not current proof.
- Added isolated synthetic transaction, numerical and label contracts plus a
  component test using unchanged original quote protection. One signal/three
  news updates -> one intent; stale portfolio blocks reservation; rollback,
  duplicate partial fills, restore and writer failure are exercised.
- Numerical fixtures separate p_win/p_edge and quantiles; zero-count upper bound
  is positive; ES uses fractional boundary mass. No prior fit or market MC run.
- Focused46 tests passed in0.70s, exit0. Full537 tests passed in57.77s, exit0.
  After writer-halt and general-alpha regression additions, final full regression
  is539 passed in64.92s, exit0; log full_regression_final_20260905.txt.
- Golden attempt01 failed on audit tool's wrong rules location; attempts02/03
  found original save envelope differences. Attempt04 applies only original
  run_id/time serialization and matches all A/B events, trades and equity, exit0.
  Failures remain in their original output directories. No strategy/source tuning.
- Original10R implementation located but denominator/order differs from candidate;
  remains explicit transfer-equivalence blocker. Historical exposure and missing
  old CLI hash remain limitations. No unseen independent superiority claimed.
- Final A-J report: HYBRID_FIRST_DELIVERY_V1_1.md. New schema, RUNBOOK, null-bearing
  disabled contract configuration and updated traceability/blocker specs included.
- No runtime integration, paid calls, Testnet/Live changes, original DB migration,
  collector interruption or frontend edits. Final process checks retain original
  PIDs and creation times. Two-hour reminder narrowed to M0/M1, paused at delivery.

Next authorized request should address M2 PIT dataset/label contract, not start
training or change thresholds. M1 full release dependencies remain unresolved;
first-delivery audit/contracts are distinct from permission to activate models.

Final focused contract log:48 passed in0.68s, exit0. Initial and final tracked
diff SHA256 match exactly (E7BA16DC...C7F985D0). Runtime source scan has no Hybrid
imports. Environment audit found stale script-context editable discovery, while
actual cwd imports and explicit CLI ROOT resolve the current repo; B-ENV added.
Final delivery inventory captures both paths without modifying any environment.
