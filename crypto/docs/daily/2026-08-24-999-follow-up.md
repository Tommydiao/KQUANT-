# 999.txt follow-up checkpoint

## Latest checkpoint: market-structure-follow-up

- The public collector health contract now backfills provider names for an
  older running-session file from the local public-provider configuration;
  it still reports `market_data_only=true` and `paper_or_order_access=false`.
- Coverage maintenance now supports resumable scope runs with deterministic
  fragments, `--resume`, `--max-scopes` and an atomic report path. A partial
  run can be continued and the main raw index is still published only after
  the explicit all-scope merge gate passes.
- Final regression after this checkpoint: Python `220 passed`, frontend
  production build passed, read-only boundary passed, and `git diff --check`
  passed.
- Runtime verification: Crypto API is healthy on `127.0.0.1:8010`, schema 17,
  strategy `crypto_roll_v1.0.0`, and the independent public collector is still
  running. Readiness remains `NO_GO / RESEARCH_ONLY`; no external Gate was
  relaxed by this maintenance work.
- Added the optional, credential-free DefiLlama public adapter and
  `collect_defillama_evidence.py`. It records the latest global stablecoin
  supply as BTC/ETH market context and AAVE/ENA protocol TVL only; unsupported
  on-chain, holder and whale fields remain `N/A`. The provider is disabled by
  default and cannot change an EVAL decision by itself.
- API contract is now `kquant-crypto-api-2026-08-24-evidence-v5`; final
  regression after the evidence adapter is Python `228 passed` and the
  frontend production build passed.
- Four source-timed DefiLlama snapshots were collected locally for BTC/ETH
  market stablecoin supply and AAVE/ENA protocol TVL. They are partial
  evidence with `data_caution`; the external-evidence Gate remains closed.
- Added a credential-free Binance public market-structure adapter and CLI. It
  records configured-universe breadth plus ETH/BTC and SOL/BTC using the
  ticker response's own close time. BTC dominance and market regime remain
  explicit `N/A`; the snapshot stays `data_caution` and cannot authorize a
  roll action.
- Collected local BTC and ETH market-structure snapshots at
  `2026-08-24T00:16:03.099Z` and `2026-08-24T00:31:03.240Z`. BTC breadth is
  `0.730769`, ETH/BTC `0.0316692`, SOL/BTC `0.00122564`; the ETH-context
  snapshot reports breadth `0.5`, ETH/BTC `0.0316768`, SOL/BTC `0.00122148`.
  Both remain `data_caution` because BTC dominance and market regime are
  explicitly unavailable. These are public research observations only, not a
  strategy result or a live trading signal.
- Added `scripts/finish_crypto_collection_maintenance.ps1`, which waits for
  the active 24-hour collector and then runs the resumable coverage-manifest,
  fragment and publish sequence. It is fail-closed and cannot replace the
  corrupt main index with a partial result.
- Targeted regression after this change: `22 passed`; full regression is now
  `228 passed`, frontend production build passed, read-only boundary passed,
  and `git diff --check` passed.

## Completed today

- Reproduced the persisted-data coverage failure: the raw
  `data/market/_coverage_index.json` was zero-filled after an interrupted
  long-running rebuild. The hot-path coverage reader now detects NUL bytes and
  recovers only from immutable closed-K-line snapshots. It reports
  `coverage_index_status=recovered_compacted` and
  `raw_index_repair_required=true`; it never scans the raw event tree from a
  dashboard request.
- Hardened the explicit repair command with bounded Parquet footer batches.
  The previous all-files DuckDB call reached about 8.7 GB private memory on
  the local 153k-file tree and was stopped before writing. The repair path now
  checks Parquet magic bytes, isolates unreadable files, reads only row-group
  footer statistics with PyArrow, and writes only after the complete scan;
  the old index remains untouched on failure.
- On the current local archive, even a full footer walk accumulates several GB
  under Windows before publishing. It was stopped before replacement, so the
  hot path continues to use the immutable compacted recovery view and keeps
  `raw_index_repair_required=true`. This is an operational maintenance
  limitation, not evidence that raw coverage is complete.
- Added a regression test for corrupt-index recovery and kept the explicit
  full raw rebuild command separate. The recovery view is not treated as a
  repaired raw index, so the persisted coverage Gate remains `NO_GO`.
- Added `scripts/run_crypto_roll_validation.py`, which builds
  `crypto_roll_v1.0.0` inputs from the same closed-bar feature prefix used by
  replay, binds point-in-time Bayesian snapshots, preserves missing evidence,
  and persists the locked roll report.
- Added a PIT/future-bar regression for the roll dataset adapter. Optional
  derivative evidence is explicitly flagged when absent; it is never filled
  with synthetic funding or OI values.
- Added `scripts/verify_crypto_backup.py` and a manifest-writing restore
  verification. The latest local backup now has matching source/restored SHA,
  `PRAGMA quick_check=ok`, 47 user tables and
  `restore_verified=true`.
- Added source lineage to external evidence (`source_version`, `collected_at`)
  and per-category asset scope, so a missing ETF/on-chain/whale feed cannot be
  counted as coverage for an unrelated asset.
- Added `scripts/capture_crypto_shadow.py`, which only captures persisted
  `allowed_shadow=true` EVAL results with an explicit point-in-time cutoff;
  it reports duplicates/skips and always records zero synthetic calendar days.
- Added the formal 999 readiness report at
  `/api/operations/go-no-go` and `scripts/check_crypto_readiness.py`; it
  aggregates every required Gate and remains `NO_GO` when any evidence is
  missing. It cannot change EVAL release flags.
- Added public `/api/version` and a Roll Journal read alias. OCR is now a
  persisted, expiring preview; a journal write requires its preview ID and an
  exact confirmation payload (or an explicit non-OCR confirmation flag).

## Real evidence observed

- A public collection run completed for BTC/ETH/SOL on both Binance and OKX;
  six exchange-derivatives snapshots were persisted with source timestamps
  and content hashes. The `crypto_public_evidence_v1.2.0` collectors now
  derive active buy/sell volume and CVD from the public trade feeds, collect
  Binance public liquidation summaries when available, and derive OKX basis
  from its public mark and index prices. The latest OKX snapshots contain
  8/9 required fields and remain `data_caution` because liquidation summaries
  are not present; the Binance snapshots also remain `data_caution` when
  liquidation data is unavailable.
- Current external evidence coverage is exchange derivatives observed for
  3/7 scoped crypto assets (`BTC`, `ETH`, `SOL`, `AAVE`, `ENA`, `ZEC`, `PUMP`)
  and verified for 0/7. Market-structure evidence is now observed for BTC
  the BTC/ETH scope, with breadth and ETH/BTC/SOL/BTC populated but `btc_dominance` and
  `market_regime` still `N/A`; no category is yet verified.
- Closed snapshot coverage: 29 CEX spot symbols; 1H spans 2026-01-01 through
  2026-08-23 with 5,617 bars per stream. 1m history is about 20 days for the
  three core symbols and about one day for most other symbols.
- Existing derivatives validation baseline (not roll performance): 32 locked
  test outcomes, win rate 53.1%, average `0.251R`, PF 1.50, maximum drawdown
  4.31R; the bootstrap Expected-R interval is `[-0.246R, 0.745R]`, so it is
  limited evidence and not a live win rate.
- `crypto_roll_v1.0.0` replay: 59 test outcomes, win rate 54.2%, average
  `0.143R`, PF 1.69, average win/loss ratio 1.43, maximum drawdown 3.29R;
  bootstrap Expected-R interval `[-0.041R, 0.350R]`. OOS summary has 121
  outcomes, average `0.071R`, PF 1.33, but its lower bootstrap bound is also
  negative. The roll Gate is `NO_GO` because sample count, Expected-R,
  win/loss ratio and bootstrap lower bound fail.

## Still blocked

- No real ETF or whale feed is configured; most on-chain/protocol fields remain
  `N/A` or `data_caution`, and the public market-structure snapshot is not a
  substitute for the missing dominance/regime evidence.
- The local database is now running schema 17/17 after service restart; no
  existing rows were deleted or rewritten.
- Roll and Monte Carlo inputs now fail closed for ETHU/MSTU/MSTR unless the
  exact listed instrument is present and marked as actual data; underlying
  return-times-two substitutions are rejected.
- Bayesian posterior records now include the feature-order hash, optional
  point-in-time training window, training dataset hash and seed; a future
  training-window end is rejected at the API boundary.
- Bayesian responses now expose the registered likelihood evidence and list
  unsupported features; unsupported features keep the result in
  `data_caution`.
- Roll inputs now require the canonical asset identity and frozen strategy
  version; mismatches become `DATA_BLOCKED` before any capital calculation.
- Monte Carlo results now persist a normalized input hash and target regime,
  and the unified gateway landing page shows live Stocks/Crypto health without
  merging sessions or data.
- The public evidence endpoint now supports an independent OKX SWAP snapshot
  beside Binance. Each public endpoint is evaluated independently and partial
  responses stay `data_caution`; no account or order endpoint is used.
- Public flow evidence is now versioned as `crypto_public_evidence_v1.2.0`;
  missing trade or liquidation endpoints remain missing rather than being
  synthesized.
- The latest persisted OKX BTC/ETH/SOL rows are `source_status=complete`,
  `trust_status=data_caution`, with `cvd`, active buy/sell volume, basis,
  funding, open interest, spread and depth. No liquidation value was inferred.
- Added an optional, key-gated CoinGlass adapter and authenticated collection
  route for derivatives, BTC/ETH ETF, on-chain and whale evidence. The adapter accepts only
  explicit provider fields, never maps long/short notional to aggressor flow,
  and returns an empty `data_caution` snapshot when the provider is disabled,
  the key is missing, or the category is unsupported.
- Extended the CoinGlass on-chain adapter to the documented stablecoin market-cap
  time series for BTC/ETH market snapshots. It reads only the latest aligned
  `data_list/time_list` point, records that point's source time, and leaves the
  field `N/A` for altcoins or malformed/missing series; no per-asset attribution
  is inferred.
- ETHU/MSTU/MSTR listed-instrument history is not present in the crypto
  Parquet dataset; no underlying-times-two substitution is used.
- No 100 qualifying roll test trades, no 15 real trading-day Shadow run, and
  no protected Postgres Staging environment exist yet.
- CoinGlass is not enabled in this environment; no CoinGlass key was added or
  written to the database, logs, responses or repository. The new on-chain and
  whale paths remain uncollected until an explicit key and provider plan are
  configured.

## Gate

`RESEARCH_ONLY -> PAPER_ONLY -> SHADOW_ONLY` remains unchanged. The new
replay and backup verification improve auditability but do not authorize
alerts, Paper positions or execution by themselves.

## Final verification for this checkpoint

- Commits: `df3dd36`, `62e763e`, `336ba13`, `4e36bac`, `1ff0a5e`, `c1384ef`,
  `1499a85`, `0fdbce9`, `c023691`, `f35efff`, `706e65f`, `48b16b4` and
  `8c3d9b7` follow the earlier roll/evidence checkpoint commits on
  `codex/crypto-v1-foundation`; the working tree is clean.
- Python regression: `228 passed` after the ETF history, stablecoin series and
  EVAL-gated Shadow capture regression cases were added.
- Frontend Vitest: `1 passed`; Vite production build succeeded with the
  generated bundle at about 218 KB before gzip after adding the evidence
  coverage panel.
- Compile, `verify_read_only_boundary.py` and `git diff --check` passed.
- After restart, `/api/version` reports application `0.3.3`, API contract
  `kquant-crypto-api-2026-08-24-evidence-v5`, Schema 17,
  `crypto_roll_v1.0.0`, `research_only=true` and `order_submission=false`;
  the unified gateway reports Stocks/Crypto available with
  `data_mixing=false` and `order_submission=false`.
- Manifest, Service Worker and the protected Roll Journal route returned the
  expected content types/statuses (`200`, `200`, `401`).
- Frontend Vitest: `1 passed`; Vite production build succeeded.
- Compile, `verify_read_only_boundary.py` and `git diff --check` passed.
- `python scripts/capture_crypto_shadow.py --limit 10` returned
  `eligible_evaluations=0`, `created=0`, and `synthetic_days_created=0`; the
  real 15-day Shadow clock has not started.
- Runtime: `http://127.0.0.1:8010/api/version` reports schema `17` and
  `crypto_roll_v1.0.0`; `http://127.0.0.1:8020/` serves the unified gateway
  page and `/api/gateway/health` reports both Stocks and Crypto available,
  with `data_mixing=false` and `order_submission=false`.
- The protected Roll Journal endpoint returns `401` without a local session;
  the public version, manifest and Service Worker endpoints return `200`.
- Direct ETHU replay without actual listed-instrument data returns
  `DATA_BLOCKED`; no underlying-times-two series is accepted.
- The explicit raw coverage rebuild was attempted with bounded DuckDB batches,
  then with filtered footer metadata and PyArrow. The current 153,611-file
  archive still accumulates several GB during a single-process full walk, so
  each attempt was stopped before index replacement. No raw file was deleted
  or rewritten; the next maintenance implementation must use OS-level
  per-partition worker processes before this Gate can be reconsidered.
- The current implementation now detects missing Parquet magic bytes,
  isolates unreadable files, reports `coverage_index_status=partial`, and
  exposes bounded unreadable-file audit details. The fixture regression for
  this path is included in the `213 passed` run.
- The maintenance path now uses a streaming file enumerator plus short-lived
  `kquant_crypto.parquet_coverage_worker` processes. This keeps the parent
  process near tens of MB and bounds native footer memory per worker. A real
  full-tree run still exceeded the 15-minute maintenance window during raw
  file enumeration, so the persisted index was intentionally not replaced;
  partition/date-scoped runs are the next operational step.
- Added resumable `venue/market_type/symbol/date` coverage fragments and an
  explicit merge gate. A scoped scan now writes only
  `data/market/_coverage_fragments/coverage-<scope>.json`; the main index can
  be published only with a complete expected-scope manifest. A real Coinbase
  BTC-USD smoke scanned 631 files and 6,146 events with `scan_status=complete`
  while leaving the corrupt main index untouched.
- Added the same backup-root lookup to the readiness CLI used by the API:
  the verified `work/backups` restore is now reported as
  `restore_verified=true`. Overall readiness remains `NO_GO` because the raw
  index, continuous collection, locked OOS validation, external evidence,
  PostgreSQL staging and 15-day Shadow gates are still incomplete.
- Hardened the public collector against a corrupt coverage index. New raw
  events remain append-only while the corrupt index is left untouched, and a
  full index rebuild is now opt-in via `--rebuild-index`; the normal long run
  no longer blocks at shutdown on an unbounded raw-tree scan.
- Added `crypto_raw_storage_v1.0.0`: the live buffer still consumes every
  trade for CVD and large-trade logic, while Parquet stores one minute-level
  `trade_summary` per instrument plus sampled BBO/ticker records. This avoids
  treating an uncontrolled tick dump as a valid 24-hour collection run.
- Limited Binance/OKX high-frequency subscriptions to the configured core
  set (`BTCUSDT`, `ETHUSDT`, `SOLUSDT` by default); the broader CEX universe
  retains ticker and 1m K-line coverage. The provider subscription regression
  confirms non-core symbols do not receive trade/BBO/mark-price streams.
- Aligned the readiness CLI with the API's bounded recovery view. It now
  reports `coverage_index_status=recovered_compacted` together with
  `raw_index_repair_required=true`, rather than treating the corrupt raw
  index as if no recovery snapshot existed.
- Raised the production collector write batch from 200 to 5,000 staged
  events. This keeps the same event and summary semantics while avoiding a
  new small-file explosion during the real 24-hour collection window.
- Added a compact public `collector_session` health field. The dashboard now
  shows whether the separate collector is running or completed without
  merging its provider state into the dashboard runtime or exposing any
  account capability.

## 999 remaining-work checkpoint

- Added `scripts/collect_999_public_evidence.py`, a repeatable public-data
  batch for Binance/OKX derivatives, Binance market structure and DefiLlama
  BTC/ETH/AAVE/ENA context. It writes only source-timed evidence snapshots;
  provider failures remain `N/A`/`data_caution` and are isolated per asset.
- The first live batch wrote Binance/OKX derivatives snapshots for BTC, ETH,
  SOL, AAVE, ENA, ZEC and PUMP, plus DefiLlama stablecoin/protocol snapshots.
  The batch is `partial`: Binance market structure was rate-limited and
  liquidation fields were missing. This is an evidence limitation, not a
  reason to relax EVAL.
- A retry with the corrected configured universe succeeded for BTC and ETH
  Binance market structure. It now contains explicit breadth and ETH/BTC plus
  SOL/BTC values, while `btc_dominance` and `market_regime` remain `N/A` by
  contract; the snapshots therefore remain `data_caution`.
- Added failure-isolation tests for the batch collector and extended the
  collection maintenance script so future 24-hour runs can execute public
  evidence collection and the locked `crypto_roll_v1` replay after coverage
  publication. A validation exit of `2` is recorded as `NO_GO`, never treated
  as permission to publish a trading action.
- The current real collector is still running. Until it finishes and the
  resumable scope manifest publishes a complete index, `raw_data_index` and
  `collection_gate` remain failed. PostgreSQL staging is not configured and
  the real 15-trading-day Shadow clock remains at zero.

- After the batch collector changes, the full Python regression is `230 passed`;
  the frontend build and read-only boundary scan remain green.
- The prior 24-hour collector stopped after about 14.3 hours without a final
  report, leaving a stale `running` marker. Commit `899c74a` adds shared
  `running/completed/stale/not_collected` classification, a stale-heartbeat
  fail-closed maintenance path, and a secret-free failure report for future
  collector exceptions. The API now exposes this session as `stale`, not live.
- The repaired maintenance process is currently scanning the resumable
  coverage scopes. It has not published the main index yet; no partial index
  is being presented as a completed 24-hour collection.
