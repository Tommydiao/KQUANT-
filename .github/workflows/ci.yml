name: KQUANT CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: web/package-lock.json
      - name: Install Python dependencies
        run: python -m pip install -e ".[dev]"
      - name: Run Python tests
        run: python -m pytest -q
      - name: Install frontend dependencies
        working-directory: web
        run: npm ci
      - name: Run frontend static checks
        working-directory: web
        run: npm run lint
      - name: Run frontend tests
        working-directory: web
        run: npm test
      - name: Build frontend
        working-directory: web
        run: npm run build
      - name: Verify read-only runtime boundary
        run: python scripts/verify_read_only_boundary.py

  verify-crypto:
    runs-on: windows-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: crypto
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: crypto/pyproject.toml
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: crypto/web/package-lock.json
      - name: Install Crypto Python dependencies
        run: python -m pip install -e ".[dev]"
      - name: Run Crypto Python tests
        run: python -m pytest -q
      - name: Install Crypto frontend dependencies
        working-directory: crypto/web
        run: npm ci
      - name: Run Crypto frontend tests
        working-directory: crypto/web
        run: npm test -- --run
      - name: Build Crypto frontend
        working-directory: crypto/web
        run: npm run build
      - name: Verify Crypto read-only runtime boundary
        run: python scripts/verify_read_only_boundary.py
